export enum BaseHandlerType {
    Command,
    Event,
    Action
}

enum BotHandlerType  {}

export enum SceneHandlerType {
    Enter = 100,
    Leave = 101
}

export enum EntityType {
    Bot,
    Scene
}